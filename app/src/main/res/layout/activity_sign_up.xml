<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SignUp.SignUpActivity">

    <RelativeLayout
        android:id="@+id/relative_signUp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tv_signUp_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_marginStart="24dp"
            android:layout_marginTop="14dp"
            android:fontFamily="@font/vazir_bold"
            android:text="ثبت نام"
            android:textColor="@color/colorPrimary"
            android:textSize="18sp"
            android:textStyle="bold" />

        <View
            android:layout_width="wrap_content"
            android:layout_height="2dp"
            android:layout_alignStart="@id/tv_signUp_title"
            android:layout_alignEnd="@id/tv_signUp_title"
            android:layout_alignBottom="@id/tv_signUp_title"
            android:background="@color/colorSecondary" />

    </RelativeLayout>

    <FrameLayout
        android:id="@+id/frame_signUp_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/relative_signUp" />

    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/animation_success"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerInParent="true"
        android:visibility="gone"
        app:lottie_autoPlay="true"
        app:lottie_loop="false"
        app:lottie_rawRes="@raw/success_animation" />

</RelativeLayout>